fn main() {
    let x: i32 = 5;
    let y: ^i32 = &x;
    let z: ^i32 = x&;

    let mut a: i64 = 10;
    let mut b: i64 = 5;
    let mut c: i64;
    let mut d: bool;

    // Unary operations
    c = -a;          // Unary minus
    d = !!(a==b);    // Logical NOT
    c = !b;        // Bitwise NOT
    c = ^&a;       // Dereference and address-of cancel out

    /*
    // Binary operations
    c = a + b;   // Addition
    c = a - b;   // Subtraction
    c = a * b;   // Multiplication
    c = a / b;   // Division
    c = a % b;   // Modulus
    c = a << 2;  // Left shift
    c = a >> 1;  // Right shift
    c = a & b;   // Bitwise AND
    c = a | b;   // Bitwise OR
    c = a ^ b;   // Bitwise XOR
    c = a && b;  // Logical AND
    c = a || b;  // Logical OR
    
    // Assignment operations
    a += 5;  // a = a + 5
    b *= 2;  // b = b * 2
    
    // Ternary operator
    c = (a > b) ? a : b;
    */
}

/*
fn plus5(arg1: i32) -> i32 {
    return arg1 + 5;
}

fn plus5(arg1: i32, arg2: bool, arg3: i64, arg4: u8) -> i32 {
    return arg1 + 5;
}


fn main() {
    let num: i32 = 1;
    let numplus5: i64 = plus5(num);
    let test_func: i64 = test(123, 22, 3);
}

fn test(mut arg1: i32, arg2: usize, arg3: u8) -> i32 {
    if 5 == 5 {
        return 5;
    } else if 7 == 10 {
        return 5 * 2;
    } else {
        return 12;
    }
}

fn void_test() {
    return;
}
