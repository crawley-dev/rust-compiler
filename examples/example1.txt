type test = bool;

fn example0_1234() -> i32 {
    return 10;
}

fn example3_1234(ident1: bool) -> i32 {
    return 5;
}

fn example1() -> bool {
    let fib: i64 = fibonacci(5);

    return true;
    /*

    let a: u32 = 0;
    //let b: u64 = a + 10;
    //let mut y: u32 = 20;
    //y += 10; // this is one crazy error message!
    //let x: u32 = (10 - 2) * 3 + 5 / (3 + 5);

    if true {
        let inner_scope_var_i64: i64 = inner_scope_var;


        let outer_scope_var: i64 = -5 + inner_scope_var;
        return inner_scope_var_i64 == outer_scope_var;
    } 
    else {
        return true;
    }
    */
}

fn fibonacci(n: i32) -> i32 {
    if n == 0 {
        return 0;
    } else if n == 1 {
        return 1;
    } else {
        return fibonacci(n-1) + fibonacci(n-2);
    }
}